<div class="all-decisions">
    <h3>My decisions</h3>
    <hr class="p3-separateur">
    <div class="all-decisions-display">
        {% if decisions is not empty %}
            {% for decision in decisions %}
                {% include 'home/blockForOneDecision.html.twig' %}
            {% endfor %}
        {% else %}
            <h4>You have not created any decision yet</h4>
        {% endif %}
    </div>
</div>
